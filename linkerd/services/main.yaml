apiVersion: v1
kind: Service
metadata:
  name: kube-service-mesh-main-service
  namespace: kube-service-mesh-linkerd
spec:
  selector:
    app: kube-service-mesh-main
  type: ClusterIP
  ports:
  - name: service
    port: 3000
